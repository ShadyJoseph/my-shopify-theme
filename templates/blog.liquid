{%paginate collections by 4 %}
<h1>{{ blog.title }}</h1>
{% for article in blog.articles %}
    <div>
    <a href="{{ article.url }}"></a>
    <h2>{{ article.title }}</h2>
    <p>{{ article.author }}</p>
    <div>{{ article.excerpt }}</div>
    </div>
{% endfor %}
{% endpaginate %}